# URL ของ backend API สำหรับ production
# ตอน dev ไม่ต้องตั้ง — ปล่อยให้ใช้ Vite proxy (/api -> http://localhost:4000)
# ตอน deploy ตั้งเป็น origin จริงของ backend เช่น https://api.gehribbon.com/api
VITE_API_URL=https://your-backend-domain.com/api
